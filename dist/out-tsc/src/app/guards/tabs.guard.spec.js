import { TestBed, inject } from '@angular/core/testing';
import { TabsGuard } from './tabs.guard';
describe('TabsGuard', function () {
    beforeEach(function () {
        TestBed.configureTestingModule({
            providers: [TabsGuard]
        });
    });
    it('should ...', inject([TabsGuard], function (guard) {
        expect(guard).toBeTruthy();
    }));
});
//# sourceMappingURL=tabs.guard.spec.js.map